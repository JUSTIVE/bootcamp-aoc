type passport

let parsePassport:string=>array<passport>

let validateField:passport=>bool

let validate:array<passport> => array<passport>

let goal1:string=>array<passport>

let goal2:string=>array<passport>
