module Tuple2 = {
  let multiplySelf = ((x, y)) => x * y

  let sumSelf = ((x, y)) => x + y
}

module Tuple3 = {
  let multiplySelf = ((x, y, z)) => x * y * z

  let sumSelf = ((x, y, z)) => x + y + z  
}